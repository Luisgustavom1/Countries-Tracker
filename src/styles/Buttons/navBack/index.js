import styled from "styled-components";
import colors from "../../colors";
import Buttons from '../button'

const Nav = styled(Buttons)`
    width: 120px;
    height: 30px;
    grid-area: nav;
`
export default Nav